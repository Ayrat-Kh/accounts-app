import { Text, View } from 'react-native';

import { Button } from '~/screens/ui/Button';

export const Login = () => {
  return (
    <View>
      <Button>
        <Text>Google login</Text>
      </Button>
    </View>
  );
};
